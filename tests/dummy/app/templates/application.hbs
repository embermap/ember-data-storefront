<h2 id="title">Welcome to Ember Data Storefront</h2>

<p>The post {{model.id}} has a title of {{model.title}}</p>


{{!-- {{outlet}}

<div>
  <strong>Posts: {{model.allPosts}}</strong>:
</div>
<div>
  {{#each model.allPosts as |post|}}

    <div>
      Post[{{post.id}}]: {{post.title}}
    </div>

  {{/each}}
</div>

<button {{action "reloadAllPosts"}}>
  Reload all posts
</button> --}}
